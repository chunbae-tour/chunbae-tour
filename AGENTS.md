# AGENTS.md

이 파일은 AI 에이전트가 `chunbae-tour` 프로젝트에서 작업을 시작할 때
가장 먼저 읽는 진입점이다.

긴 규칙은 이 파일에 모두 넣지 않는다. 에이전트는 작업 유형을 판단한 뒤
`docs/agent-harness/00-READING-MAP.md`에 따라 필요한 문서만 추가로 읽는다.
하네스 문서 추가와 배치 규칙은 `docs/agent-harness/README.md`를 따른다.

## 시작 절차

1. 사용자 요청을 작업 유형으로 분류한다.
2. `docs/agent-harness/00-READING-MAP.md`를 읽는다.
3. 모든 작업에서 `docs/agent-harness/01-AGENT-HARNESS.md`를 읽는다.
4. 작업 유형에 맞는 문서만 추가로 읽는다.
5. 금지 작업이나 승인 필요 작업이 있으면 진행하지 않고 사용자에게 보고한다.

## 절대 원칙

- `main` 브랜치에 직접 push하지 않는다.
- PR을 자동으로 merge하지 않는다.
- secret, API key, password를 코드에 하드코딩하지 않는다.
- 사용자 변경사항을 임의로 되돌리지 않는다.
- 보호 파일은 사용자 승인 없이 수정하지 않는다.
- DB 스키마 변경은 사용자 승인 후 진행한다.
- 외부 API 추가나 비용 발생 작업은 사용자 승인 후 진행한다.

## 작업 완료 보고

작업을 마치면 다음을 간단히 보고한다.

- 변경한 파일
- 실행한 검증 명령
- 실패한 검증이 있다면 이유
- 사용자 확인이 필요한 사항
