# AGENTS.md — 춘배투어 (ChunBae Tour)

이 문서는 AI 에이전트가 본 프로젝트에서 작업할 때 반드시 읽고 따라야 하는 기준입니다.
작업 시작 전 전체 내용을 숙지하고, 아래 규칙을 엄격하게 준수하십시오.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | 춘배투어 (ChunBae Tour) |
| **유형** | 위치기반 관광지·전통시장·투어 플랫폼 (웹 PWA) |
| **개발 기간** | 2026.05.12 ~ 2026.06.22 (총 6주) |
| **팀 구성** | 백엔드 5명 |
| **중간 발표** | 2026.05.29 |
| **최종 발표** | 2026.06.23 |

### 서비스 한 줄 요약

사용자 현재 위치를 기반으로 주변 관광지·전통시장 정보를 제공하고, 길찾기·축제 캘린더·커뮤니티·동행 채팅·엽전 결제를 통해 외국인과 국내 여행자 모두에게 편리한 여행 경험을 제공하는 플랫폼.

### 기술 스택

| 영역 | 기술 |
|------|------|
| 언어 / 프레임워크 | Java 21, Spring Boot 4.0.6 |
| 데이터 접근 | Spring Data JPA, QueryDSL |
| 데이터베이스 | MySQL (AWS RDS) |
| 캐시 / 메시징 | Redis (ZSet, Geospatial, Pub/Sub, 분산 락) |
| 실시간 통신 | WebSocket, STOMP |
| 인증 / 보안 | JWT, Spring Security |
| 인프라 | AWS EC2, RDS, ElastiCache, ECR, S3 |
| CI/CD | GitHub Actions, Docker |
| 프론트엔드 | React, PWA |
| 외부 API | KakaoMap API, PG사, Google Cloud Translation API |

### 담당 도메인 (박경화 파트)

박경화 파트 명세는 `docs/specs/박경화_*.md` 문서들로 분리되어 있다.
작업 시작 시 `박경화_00_공통규칙.md`와 작업 도메인에 해당하는 `박경화_*` 문서를 먼저 참조한다.

| 도메인 | 주요 기능 |
|--------|---------|
| **Community** | 동행 게시판, 자유 게시판, 댓글(대댓글) |
| **Place** | 관광지 리뷰 |
| **Festival** | 축제 목록 조회, 월별 캘린더 조회 (관리자가 등록·관리) |
| **Report** | 사용자 신고 생성, 관리자 신고 처리 |

### 액터 및 Role

| Role | 설명 |
|------|------|
| `USER` | 일반 로그인 사용자 (내·외국인 여행자) |
| `MERCHANT` | 관리자 승인으로 상인 권한을 부여받은 사용자 |
| `ADMIN` | 서비스 운영 관리자 |

### Base URL

```
/api/v1
```

---

## 2. 코드 규칙

- **함수명**: `snake_case`
- **클래스명**: `PascalCase`
- **한 함수는 하나의 역할만 수행한다** — 여러 역할이 필요하면 함수를 분리한다
- **타입 힌트 필수** — 모든 함수의 인자와 반환 타입에 타입 힌트를 명시한다

---

## 3. 코드 작성 전 필수 확인

코드를 작성하기 전에 **반드시** 아래 내용을 사용자에게 먼저 보고하고 확인을 받는다. 확인 없이 코드를 작성하지 않는다.

1. 구현하려는 내용 요약
2. 생성하거나 수정할 파일 목록
3. 참조한 문서와 섹션

사용자가 확인("응", "맞아", "진행해" 등)한 이후에만 코드를 작성한다.

---

## 4. 절대 금지

| 금지 사항 | 이유 | 대안 |
|---------|------|------|
| `src/` 파일 수정 | 레거시 코드. 건드리면 장애 발생 | 신규 기능은 별도 모듈로 작성 |
| `print()` 사용 | 운영 환경 로그 오염 | `logger.info()` 사용 |
| 직접 DB 쿼리 | 데이터 정합성 보장 불가 | 반드시 서비스 레이어 경유 |

---

## 5. PR 규칙

- **커밋 메시지 접두사** 필수 사용:
  - `feat:` 새 기능 추가
  - `fix:` 버그 수정
  - `refactor:` 기능 변경 없는 코드 개선
- **PR 하나에 하나의 변경만** — 여러 변경이 필요하면 PR을 분리한다
- **테스트 없는 PR은 올리지 않는다**

---

## 6. 테스트

- 테스트 파일 위치: `tests/` 디렉토리
- 새 기능에는 반드시 테스트를 추가한다
- 테스트 없이 구현만 완료된 코드는 미완성으로 간주한다

---

## 7. 주요 설계 원칙 (작업 전 숙지)

- **Controller → Service만 호출**, 비즈니스 로직을 Controller에 두지 않는다
- **Entity를 Controller 응답으로 직접 반환하지 않는다** — 반드시 DTO 변환 후 반환
- **삭제는 Soft Delete 기본** — `deletedAt` 필드로 관리하며 물리 삭제하지 않는다
- **상태값은 Enum으로 관리**한다
- **DTO는 해당 DTO를 소비하는 Service가 속한 도메인의 `dto` 패키지에 둔다**
- **사용자용/관리자용 API는 같은 도메인 패키지 안에서 Controller를 분리한다**
- **페이징은 Cursor 기반**으로 구현한다 (`cursor`, `size` 파라미터)

---

## 8. 참조 문서 — 필요 시에만 읽는다

아래 문서들은 매번 읽지 않는다.
**모든 작업에서 정보를 찾을 때는 `docs/specs/박경화_*.md` 문서를 먼저 확인한다.**
박경화 파트 문서에 없는 타 도메인 정보가 필요할 때만 아래 문서들을 추가로 읽는다.
문서 파일명 뒤에 해시가 붙은 공통 명세는 `rg --files docs/specs`로 정확한 파일명을 확인한 뒤 읽는다.

---

### 항상 먼저 읽는다
- `docs/specs/박경화_00_공통규칙.md` — 공통 응답, 예외, 페이징, BaseEntity, 패키지 구조 원칙
- 작업 도메인에 해당하는 문서만 추가로 읽는다. 전체를 읽지 말고 관련 섹션만 읽는다.
  - `docs/specs/박경화_01_Community.md` — Community 게시글·댓글
  - `docs/specs/박경화_02_Place_리뷰.md` — Place 관광지 리뷰
  - `docs/specs/박경화_03_Festival.md` — Festival 축제·캘린더
  - `docs/specs/박경화_04_Report.md` — Report 신고
  - `docs/specs/박경화_05_10_UC매핑_QueryDSL_타도메인_보안_S3_동시성.md` — 유스케이스 매핑, QueryDSL, 타 도메인 연결, 보안, S3, 동시성

---

### 박경화 문서에 없는 경우에만 추가로 읽는다

**API 구현 / 타 도메인 연동이 필요할 때**
- `docs/specs/06_API_명세서*.md` — 전체 프로젝트 API 명세 (타 도메인 엔드포인트 확인 시)

**패키지 / 클래스 생성 위치가 불확실할 때**
- `docs/specs/13_백엔드_패키지_구조_설계서*.md`

**에러 응답 / 예외 처리 작업 시**
- `docs/specs/12_공통_에러코드_설계서*.md`

**동시성 처리 관련 작업 시**
- `docs/specs/09_동시성_제어_설계서*.md`

**화면 연동 / 프론트엔드 요청 파라미터 확인 시**
- `docs/specs/07_화면_설계서*.md`
- `docs/specs/DESIGN_SPEC_ChunBaeTour.md`

**CORS / 보안 / Rate Limit 관련 작업 시**
- `docs/specs/11_운영_보안_정책_설계서*.md`

**인프라 / 배포 / 환경변수 관련 작업 시**
- `docs/specs/08_인프라_아키텍처_다이어그램*.md`
- `docs/specs/10_ADR*.md`

**프로젝트 전체 맥락 / 유스케이스 확인이 필요할 때**
- `docs/specs/01_프로젝트_개요서*.md`
- `docs/specs/02_사용자_시나리오*.md`
- `docs/specs/03_유스케이스_명세서*.md`
