# 14. Jira 이슈 목록 — 스토어 / 결제 / 상인 / 운영 (신현민)

> 담당 Epic: **스토어 / 결제 / 상인 / 운영 도메인 구현**
> 작업 완료 후 이 파일 기준으로 해당 이슈 Jira에 등록.

---

## 등록 순서 (의존성 맵)

```
STORY-01
  └─ STORY-02
        ├─ STORY-03 → STORY-04
        ├─ STORY-05
        ├─ STORY-06 → STORY-07
        └─ STORY-13 → STORY-14 → STORY-15

STORY-08 → STORY-09 → STORY-10
                          ├─ STORY-11
                          ├─ STORY-12 → STORY-13
                          ├─ STORY-19
                          ├─ STORY-20
                          └─ STORY-21

STORY-01 → STORY-16 → STORY-17 → STORY-18
```

**블로커 이슈를 먼저 등록**하고, Jira "Linked Issues → is blocked by" 필드로 연결.

---

## STORY-01 | [Infra] Redis/Redisson 설정 + 도메인 ErrorCode 등록

| 항목 | 내용 |
|------|------|
| Priority | Highest |
| Story Points | 3 |
| Labels | backend, infra |
| Blocked by | 없음 — 가장 먼저 등록 |
| 상태 | ✅ 완료 (KAN-45) |

**구현 목표**
RedissonClient 빈 등록, PAY/STORE/MERCHANT/SHOP ErrorCode enum 추가.
이후 모든 결제·동시성 구현의 전제 조건.

**완료 기준**
- [ ] RedissonClient 빈 정상 등록, 로컬 Redis 연결 확인
- [ ] StringRedisTemplate 빈 등록
- [ ] ErrorCode enum에 PAY_001~011, STORE_001~007, MERCHANT_001~003, SHOP_001~006 추가
- [ ] 단위 테스트: ErrorCode 잘못된 사용 시 컴파일 에러

---

## STORY-02 | [Payment] Wallet 엔티티 + 엽전 잔액 조회 API

| 항목 | 내용 |
|------|------|
| Priority | Highest |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | STORY-01 |
| 상태 | ✅ 완료 (KAN-48) |

**구현 목표**
wallets 테이블 엔티티 생성. GET /api/v1/yeopjeon/me로 인증된 사용자 엽전 잔액 반환.
가입 시 자동 생성은 UserRegisteredEvent 활용 (정민교 협의).

**완료 기준**
- [ ] Wallet 엔티티 (userId FK, balance BIGINT)
- [ ] GET /api/v1/wallets/me → 잔액 + userId 반환 (USER 인증 필수)
- [ ] 미존재 userId 조회 시 404
- [ ] 단위 테스트: 잔액 조회 정상/미존재 케이스

---

## STORY-03 | [Payment] 엽전 충전 요청 → PG 결제창 리다이렉트 (멱등성 포함)

| 항목 | 내용 |
|------|------|
| Priority | Highest |
| Story Points | 8 |
| Labels | backend, mvp, payment |
| Blocked by | STORY-01, STORY-02 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/payments/charge 수신 → 멱등성 키 중복 검사 → PG사 주문 생성 → 결제창 URL 반환.
PG사 인터페이스는 PaymentGatewayClient로 추상화 (Toss/Kakao 미결정).

**완료 기준**
- [ ] PaymentGatewayClient 인터페이스 + Stub 구현체
- [ ] IdempotencyService: Redis TTL 24h, 중복 시 PAY_007
- [ ] 충전 금액 유효성: 최소 1,000원(PAY_002), 단위 1,000원(PAY_003), 최대 100,000원(PAY_004)
- [ ] payment_orders 레코드 생성 (status: PENDING)
- [ ] 응답: PG 결제창 redirectUrl
- [ ] 단위 테스트: 멱등성 중복, 금액 유효성 실패 케이스

---

## STORY-04 | [Payment] PG 결제 콜백 처리 → 엽전 잔액 증가

| 항목 | 내용 |
|------|------|
| Priority | Highest |
| Story Points | 5 |
| Labels | backend, mvp, payment |
| Blocked by | STORY-03 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/payments/callback/success 수신 → PG 서명 검증 →
payment_orders 상태 COMPLETED → Wallet 잔액 증가 → yeopjeon_histories INSERT.

**완료 기준**
- [ ] PG 콜백 서명 검증 로직 (위변조 방지)
- [ ] payment_orders status: PENDING → COMPLETED
- [ ] Wallet balance += 충전 금액 (원자적 UPDATE)
- [ ] yeopjeon_histories: type=CHARGE, amount=+N, balanceSnapshot
- [ ] POST /api/v1/payments/callback/fail → FAILED 상태 처리
- [ ] 통합 테스트: 콜백 정상/실패 플로우

---

## STORY-05 | [Payment] 엽전 이력 조회 (커서 기반 페이지네이션)

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | STORY-02 |
| 상태 | ⬜ 미등록 |

**구현 목표**
GET /api/v1/wallets/me/histories?cursor={id}&size={n}
yeopjeon_histories를 최신순으로 커서 기반 조회.

**완료 기준**
- [ ] 커서 없으면 최신 N건, 있으면 해당 id 이전 N건
- [ ] 응답: items[], nextCursor (다음 없으면 null)
- [ ] 본인 이력만 조회 가능 (타인 접근 시 PAY_011)
- [ ] 단위 테스트: 첫 페이지, 중간 페이지, 마지막 페이지

---

## STORY-06 | [Payment] 환불 요청 생성

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | STORY-02 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/payments/{orderId}/refund → 미사용 엽전 전액 환불 신청.
부분 사용 후 잔액 환불 불가 정책 적용.

**완료 기준**
- [ ] 미사용 잔액 전액 보유 확인 (전액 아니면 거절)
- [ ] refund_requests 생성 (status: PENDING)
- [ ] 중복 신청 방지 (이미 PENDING 있으면 409)
- [ ] PATCH /api/v1/payments/refund/{refundId}/cancel → CANCELLED
- [ ] 단위 테스트: 전액 보유/부족/중복 신청 케이스

---

## STORY-07 | [Payment] 관리자 환불 승인/거절 (상태 전이 락)

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 5 |
| Labels | backend, mvp, concurrency |
| Blocked by | STORY-06 |
| 상태 | ⬜ 미등록 |

**구현 목표**
GET /api/v1/admin/refunds (커서 조회) + PATCH approve/reject.
승인 시 PG사 환불 요청 + Wallet 잔액 차감.
동시 승인 방지: SELECT FOR UPDATE (refund_requests).

**완료 기준**
- [ ] GET /api/v1/admin/refunds 커서 페이지네이션
- [ ] 승인: refund_requests SELECT FOR UPDATE → APPROVED → PG 환불 요청 → wallet balance 차감
- [ ] 거절: status → REJECTED
- [ ] 이미 처리된 건 재처리 시도 시 400
- [ ] 단위 테스트: 승인/거절/중복 처리 케이스

---

## STORY-08 | [Merchant] 상인 등록 신청 API

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | 없음 — STORY-01과 병렬 진행 가능 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/merchants/apply (USER 인증) →
사업자 등록번호 + 가게 기본 정보 제출 →
merchant_applications 생성 (status: PENDING).

**완료 기준**
- [ ] 사업자 등록번호 형식 유효성 검사 (MERCHANT_002)
- [ ] 중복 신청 방지: 이미 PENDING 있으면 MERCHANT_001
- [ ] merchant_applications 저장
- [ ] 단위 테스트: 정상 신청, 중복, 형식 오류

---

## STORY-09 | [Merchant] 관리자 상인 승인/거절 + role 변경 ⚠️ HITL

| 항목 | 내용 |
|------|------|
| Priority | Highest |
| Story Points | 5 |
| Labels | backend, mvp, hitl |
| Blocked by | STORY-08 |
| 상태 | ⬜ 미등록 |

> ⚠️ **HITL**: 정민교와 AccountRepository.updateRole() 인터페이스 협의 후 구현 시작.

**구현 목표**
GET /api/v1/admin/merchant-applications → 목록 조회.
PATCH approve → user.role = MERCHANT.
PATCH reject → status = REJECTED.

**완료 기준**
- [ ] 정민교와 role 변경 인터페이스 협의 완료 (선행 조건)
- [ ] 승인: merchant_applications APPROVED + AccountRepository로 role 변경
- [ ] 거절: REJECTED + 사유 저장
- [ ] 이미 처리된 건 재처리 시도 시 400
- [ ] 통합 테스트: 승인 후 MERCHANT API 접근 가능 확인

---

## STORY-10 | [Shop] 가게 등록 + 수정

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | STORY-09 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/merchants/me/shop (가게 등록, 상인 1인 1가게).
PATCH /api/v1/merchants/me/shop (운영시간·소개글 수정, 위치 수정 불가).
GET /api/v1/merchants/me/shop.

**완료 기준**
- [ ] 1인 1가게 정책: 이미 가게 있으면 SHOP_003
- [ ] 가게명 최대 50자 (SHOP_006)
- [ ] 본인 가게만 수정 가능 (SHOP_002)
- [ ] 관리자용: GET/PATCH /api/v1/admin/shops, GET /api/v1/admin/shops/{shopId}
- [ ] 단위 테스트: 중복 등록, 권한 오류, 필드 유효성

---

## STORY-11 | [Shop] 메뉴 등록·수정·삭제 (CRUD)

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | STORY-10 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/merchants/me/shop/menus,
PATCH /api/v1/merchants/me/shop/menus/{menuId},
DELETE /api/v1/merchants/me/shop/menus/{menuId}.

**완료 기준**
- [ ] 메뉴 가격 0원 이상 (SHOP_005)
- [ ] 존재하지 않는 메뉴 수정/삭제 시 SHOP_004
- [ ] 본인 가게 메뉴만 수정 가능 (SHOP_002)
- [ ] Soft Delete 적용
- [ ] 단위 테스트: CRUD 정상 + 권한 오류

---

## STORY-12 | [Shop] QR 코드 발급

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 2 |
| Labels | backend, mvp |
| Blocked by | STORY-10 |
| 상태 | ⬜ 미등록 |

**구현 목표**
GET /api/v1/merchants/me/qr → 상인 가게 고유 QR 코드 URL 반환.
GET /api/v1/shops/{shopId}/qr-info → 가게명 + 메뉴 목록 (비인증 공개).

**완료 기준**
- [ ] QR는 shopId 기반 고정 URL 생성
- [ ] GET /api/v1/shops/{shopId}/qr-info: 비로그인 접근 가능
- [ ] 존재하지 않는 shop 조회 시 SHOP_001
- [ ] 단위 테스트: 정상 조회, 미존재 shop

---

## STORY-13 | [QR Pay] QR 결제 요청 생성 (사용자 측)

| 항목 | 내용 |
|------|------|
| Priority | Highest |
| Story Points | 5 |
| Labels | backend, mvp, payment |
| Blocked by | STORY-02, STORY-12 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/payments/qr {shopId, menuItems} →
qr_pay_requests 생성 (status: PENDING_CONFIRM, expiredAt: now+5분).

**완료 기준**
- [ ] 메뉴 존재 여부 확인 (SHOP_004)
- [ ] 총 결제 금액 계산 + 잔액 사전 확인 (PAY_001) — 실제 차감은 상인 승인 시점
- [ ] qr_pay_requests 저장 (status: PENDING_CONFIRM)
- [ ] GET /api/v1/merchants/me/qr-payments/pending → 상인 대기 목록
- [ ] 단위 테스트: 정상 생성, 잔액 부족 사전 체크

---

## STORY-14 | [QR Pay] QR 결제 승인/거절 (상인 측, 6단계 트랜잭션 + 분산 락)

| 항목 | 내용 |
|------|------|
| Priority | Highest |
| Story Points | 13 |
| Labels | backend, mvp, payment, concurrency |
| Blocked by | STORY-13 |
| 상태 | ⬜ 미등록 |

**구현 목표**
PATCH /api/v1/payments/qr/{payRequestId}/confirm {action: APPROVE|REJECT}.
APPROVE: 분산 락 → 비관적 락 → 사용자 잔액 차감 → 상인 잔액 증가 → 양쪽 이력 기록 → COMPLETED.

**완료 기준**
- [ ] 분산 락: qr:lock:{shopId}:{userId} (대기 3s, 임대 5s)
- [ ] SELECT FOR UPDATE 순서 고정: wallets → shop_wallets (데드락 방지)
- [ ] 잔액 부족 시 PAY_001 (분산 락 해제 후)
- [ ] yeopjeon_histories: 사용자(-), 상인(+) 양쪽 기록
- [ ] 분산 락 실패 시 PAY_008
- [ ] 이미 EXPIRED/COMPLETED 건 처리 시도 시 400
- [ ] 통합 테스트: 정상 승인, 잔액 부족, 락 충돌 시나리오

---

## STORY-15 | [QR Pay] QR 결제 만료 스케줄러 (PENDING_CONFIRM → EXPIRED)

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | STORY-14 |
| 상태 | ⬜ 미등록 |

**구현 목표**
@Scheduled 30초 주기. PENDING_CONFIRM + created_at < now()-5분 → EXPIRED.
단일 서버 가정 (다중 서버 배포 시 ShedLock 도입 필요).

**완료 기준**
- [ ] 30초 주기 배치 실행
- [ ] 만료 대상 일괄 EXPIRED 처리
- [ ] STORY-13에서 실제 차감 안 했으므로 별도 잔액 복구 불필요
- [ ] 단위 테스트: 5분 경과 건 EXPIRED 전환 확인

---

## STORY-16 | [Store] 스토어 상품 목록 + 상세 조회

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 2 |
| Labels | backend, mvp |
| Blocked by | STORY-01 |
| 상태 | ⬜ 미등록 |

**구현 목표**
GET /api/v1/store/products (목록, 비인증 공개).
GET /api/v1/store/products/{productId} (상세, 비인증 공개).
관리자: POST/PATCH/DELETE /api/v1/admin/store/products.

**완료 기준**
- [ ] 상품 목록: 재고 포함 반환 (재고 0이면 품절 표시)
- [ ] 상품 상세: 가격, 설명, 재고, 이미지 URL
- [ ] 존재하지 않는 상품 STORE_001
- [ ] 관리자 상품 등록 시 Redis stock:{productId} 초기화
- [ ] 단위 테스트: 정상 조회, 미존재 상품

---

## STORY-17 | [Store] 한정 수량 상품 구매 (3단계 동시성)

| 항목 | 내용 |
|------|------|
| Priority | Highest |
| Story Points | 13 |
| Labels | backend, mvp, concurrency |
| Blocked by | STORY-02, STORY-16 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/store/orders → 3단계 동시성 보호.
1단계: Redis DECR stock:{productId} (음수 시 INCR 복구 + STORE_002)
2단계: Redisson purchase:lock:{userId} (실패 시 INCR 복구 + STORE_005)
3단계: SELECT FOR UPDATE (products) 재고 최종 확인.

**완료 기준**
- [ ] 3단계 순서대로 실행, 각 단계 실패 시 이전 단계 보상
- [ ] 엽전 잔액 차감 (PAY_001)
- [ ] 재고 DB 확정 차감 + store_orders + user_items 저장
- [ ] 수량 유효성: 1개 이상(STORE_003), 최대 수량 초과(STORE_004)
- [ ] 통합 테스트: 동시 10명 구매 → 재고 정확히 차감 확인

---

## STORY-18 | [Store] 내 주문 내역 + 보유 아이템 조회

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 2 |
| Labels | backend, mvp |
| Blocked by | STORY-17 |
| 상태 | ⬜ 미등록 |

**구현 목표**
GET /api/v1/store/orders (내 주문 내역).
GET /api/v1/users/me/items (내 보유 아이템).

**완료 기준**
- [ ] 커서 기반 페이지네이션
- [ ] 본인 주문/아이템만 조회 가능
- [ ] 주문 상태(COMPLETED, CANCELLED) 포함 반환
- [ ] 단위 테스트: 페이지네이션 경계값

---

## STORY-19 | [Shop] 정산 신청 + 관리자 처리

| 항목 | 내용 |
|------|------|
| Priority | High |
| Story Points | 5 |
| Labels | backend, mvp |
| Blocked by | STORY-10 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/merchants/me/settlements → 정산 신청.
관리자 승인 시 ShopWallet 잔액 차감 + 실제 이체(수동).
GET /api/v1/merchants/me/settlements (내 정산 내역).

**완료 기준**
- [ ] 정산 신청: shop_wallets 잔액 > 0 확인
- [ ] settlement_requests 생성 (status: PENDING)
- [ ] 관리자 승인: ShopWallet 잔액 차감
- [ ] 관리자 거절: REJECTED
- [ ] 단위 테스트: 잔액 0 신청 거부, 정상 플로우

---

## STORY-20 | [Shop] 상인 홈 대시보드 (오늘 매출, Redis 캐싱 3분)

| 항목 | 내용 |
|------|------|
| Priority | Medium |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | STORY-10 |
| 상태 | ⬜ 미등록 |

**구현 목표**
GET /api/v1/merchants/me/home → 오늘 매출 합계 + 최근 결제 목록.
Redis TTL 3분 캐싱 (정확도보다 응답 속도 우선).

**완료 기준**
- [ ] Redis 캐시 hit: 캐시 반환
- [ ] Redis 캐시 miss: DB 조회 후 TTL 3분 캐싱
- [ ] 오늘 매출 = 당일 COMPLETED QR 결제 합계
- [ ] 최근 결제 목록 최대 10건
- [ ] 단위 테스트: 캐시 hit/miss 분기

---

## STORY-21 | [Shop] 광고 신청 + 관리자 승인/거절

| 항목 | 내용 |
|------|------|
| Priority | Medium |
| Story Points | 3 |
| Labels | backend, mvp |
| Blocked by | STORY-10 |
| 상태 | ⬜ 미등록 |

**구현 목표**
POST /api/v1/merchants/me/ads → 광고 신청.
GET /api/v1/admin/ads → 신청 목록.
PATCH /api/v1/admin/ads/{adId}/approve|reject.

**완료 기준**
- [ ] ad_applications 생성 (status: PENDING)
- [ ] 이미 PENDING 신청 있으면 중복 방지
- [ ] 관리자 승인/거절 상태 전이
- [ ] 단위 테스트: 중복 신청, 승인/거절 정상 플로우

---

## 전체 현황 요약

| Story | 제목 | SP | 상태 |
|-------|------|----|------|
| STORY-01 | Redis/Redisson 설정 + ErrorCode | 3 | ⬜ |
| STORY-02 | Wallet 엔티티 + 잔액 조회 | 3 | ⬜ |
| STORY-03 | 엽전 충전 요청 + PG 리다이렉트 | 8 | ⬜ |
| STORY-04 | PG 콜백 처리 + 잔액 증가 | 5 | ⬜ |
| STORY-05 | 엽전 이력 조회 (커서) | 3 | ⬜ |
| STORY-06 | 환불 요청 생성 | 3 | ⬜ |
| STORY-07 | 관리자 환불 승인/거절 | 5 | ⬜ |
| STORY-08 | 상인 등록 신청 | 3 | ⬜ |
| STORY-09 | 관리자 상인 승인/거절 ⚠️HITL | 5 | ⬜ |
| STORY-10 | 가게 등록 + 수정 | 3 | ⬜ |
| STORY-11 | 메뉴 CRUD | 3 | ⬜ |
| STORY-12 | QR 코드 발급 | 2 | ⬜ |
| STORY-13 | QR 결제 요청 생성 (사용자) | 5 | ⬜ |
| STORY-14 | QR 결제 승인/거절 (상인, 분산 락) | 13 | ⬜ |
| STORY-15 | QR 결제 만료 스케줄러 | 3 | ⬜ |
| STORY-16 | 스토어 상품 목록/상세 조회 | 2 | ⬜ |
| STORY-17 | 한정 수량 상품 구매 (3단계 동시성) | 13 | ⬜ |
| STORY-18 | 내 주문/보유 아이템 조회 | 2 | ⬜ |
| STORY-19 | 정산 신청 + 관리자 처리 | 5 | ⬜ |
| STORY-20 | 상인 홈 대시보드 (캐싱) | 3 | ⬜ |
| STORY-21 | 광고 신청 + 관리자 승인/거절 | 3 | ⬜ |
| **합계** | | **96 SP** | |

> 작업 완료 시 해당 행 `⬜ → ✅`로 업데이트.
