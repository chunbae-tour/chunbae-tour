# 🗺️ 관광지 / 지도 / 검색 도메인 작업 체크리스트

> **담당**: 김인목 | **핵심 기술**: Redis Geospatial, Redis ZSet, KakaoMap API, QueryDSL
> **마지막 업데이트**: 2026-05-19

---

## ✅ 이미 완료된 공통 기반 (건드릴 필요 없음)

| 항목 | 파일 위치 | 상태 |
|---|---|---|
| 공통 응답 Wrapper | `common/response/ApiResponse.java` | ✅ 완료 |
| 공통 에러 처리 | `common/error/GlobalExceptionHandler.java` | ✅ 완료 |
| 공통 에러코드 Enum | `common/error/ErrorCode.java` | ✅ 완료 (내 에러코드 추가 필요) |
| BusinessException | `common/error/BusinessException.java` | ✅ 완료 |
| JPA Auditing | `common/config/JpaAuditingConfig.java` | ✅ 완료 |
| SecurityConfig (기본) | `auth/config/SecurityConfig.java` | ✅ 완료 (JWT 필터는 정민교 작업 중) |
| MySQL + Redis Docker | `docker-compose.yml` | ✅ 완료 |
| application.yml (Redis 설정 포함) | `resources/application-local.yml` | ✅ 완료 |
| users 테이블 Entity | `auth/Account.java` | ✅ 완료 |

> [!NOTE]
> **QueryDSL은 아직 build.gradle에 없음** → PHASE 0에서 추가 필요
> **RedisConfig Bean도 없음** → PHASE 0에서 추가 필요
> **Redis Geospatial 명령은 `RedisTemplate<String, String>`** 기반으로 직접 사용

---

## 🔑 팀원 의존성 요약

| 선행 작업 | 담당자 | 내 작업에 미치는 영향 |
|---|---|---|
| JWT 인증 필터 완성 | 정민교 (작업 중) | 찜/리뷰작성/최근검색어 등 `@AuthenticationPrincipal` 필요한 API |
| `shops` 테이블 완성 | 신현민 | `GET /places/{placeId}/nearby-shops` 연동 |
| `posts` 테이블 완성 | 박경화 | places ↔ posts 연결 (place_id FK) |

> [!IMPORTANT]
> 인증 없는 조회 API가 전체의 약 70%. **지금 당장 시작 가능**하다.
> SecurityConfig에서 내 API 경로는 `permitAll()`로 임시 열어달라고 정민교에게 요청하거나 직접 추가하면 됨.

---

## PHASE 0 — 내 도메인 진입 준비 (오늘 시작)

### ~~0-1. 의존성 추가 (build.gradle)~~ ✅ 완료
- [x] **QueryDSL 추가** — `querydsl-jpa:5.1.0:jakarta` + `querydsl-apt:5.1.0:jakarta` + annotationProcessor
- [x] 빌드 후 Q클래스 생성 확인 → `QAccount.java` 정상 생성 (`BUILD SUCCESSFUL`)

### ~~0-2. RedisConfig Bean 생성~~ ✅ 완료
- [x] `common/config/RedisConfig.java` 생성
  - `StringRedisTemplate` Bean — ZSet/GEO/LIST/INCR용
  - `RedisTemplate<String, Object>` Bean — JSON 캐싱용 (`GenericJacksonJsonRedisSerializer`, Jackson 3)
  - Spring Boot 4.x = Jackson 3(`tools.jackson`) → `com.fasterxml.jackson` 사용 불가
  - `GenericJackson2JsonRedisSerializer` deprecated → `GenericJacksonJsonRedisSerializer` 사용

### ~~0-3. 내 도메인 에러코드 추가~~ ✅ 완료
- [x] SA 에러코드 설계서(12번 문서) 기준 전체 도메인 ErrorCode Enum으로 통합
  - COMMON / AUTH / USER / **PLACE / COMMUNITY** / CHAT / PAY / STORE / SHOP / MERCHANT / ADMIN 전체 추가
  - `INVALID_REQUEST` → `INVALID_INPUT_VALUE` 리팩토링 (GlobalExceptionHandler 동기화)
  - 정민교 팀원 코드에서 참조하던 `LOGIN_FAILED` → `INVALID_CREDENTIALS`, `AUTHENTICATION_REQUIRED` → `LOGIN_REQUIRED` 자동 매핑 수정

### ~~0-4. Place / Review / UserLike Entity 생성~~ ✅ 완료
- [x] `domain/place/Place.java` Entity 생성 (SA ERD 3.2 기준, DECIMAL(10,7) lat/lng, JSON imageUrls/tags)
- [x] `domain/place/PlaceCategory.java` Enum (TOURIST_SPOT, TRADITIONAL_MARKET)
- [x] `domain/place/PlaceStatus.java` Enum (ACTIVE, HIDDEN, DELETED)
- [x] `domain/place/UserLike.java` Entity 생성 (uk_user_likes_user_place UNIQUE 제약)
- [x] Review Entity 제외 — **박경화님 담당 (COMMUNITY 도메인)**

### ~~0-5. Repository 생성~~ ✅ 완료
- [x] `PlaceRepository.java` (JpaRepository)
- [x] `UserLikeRepository.java`
  - `existsByUserIdAndPlaceId(Long userId, Long placeId)` 메서드 선언

### 0-6. 패키지 구조 생성
```
domain/
  place/
    controller/
      PlaceController.java
      DirectionController.java
    service/
      PlaceService.java
      PlaceLikeService.java
      DirectionService.java
    repository/
      PlaceRepository.java
      PlaceQueryRepository.java
      UserLikeRepository.java
    entity/
      Place.java
      UserLike.java
    dto/
      request/
        NearbyPlaceRequest.java
        DirectionRequest.java
      response/
        PlaceDetailResponse.java
        NearbyPlaceResponse.java
        DirectionResponse.java
    type/
      PlaceCategory.java
      PlaceStatus.java
  search/
    controller/
      SearchController.java
    service/
      SearchService.java
      PopularSearchService.java
      RecentSearchService.java
    repository/
      SearchQueryRepository.java
    dto/
      request/
        SearchRequest.java
      response/
        SearchPlaceResponse.java
        SearchFestivalResponse.java
        PopularSearchResponse.java
        RecentSearchResponse.java
    type/
      SearchTargetType.java
```
- [x] 위 패키지 구조대로 빈 파일 생성 (Review 관련 파일은 Community 도메인이므로 제외)

---

## PHASE 1 — 핵심 P0 기능 (인증 불필요 조회 API)

> 중간 발표(5/29) 전 반드시 완료. 정민교 기다릴 필요 없음.

### 1-1. 위치 기반 관광지 탐색 `GET /api/v1/places/nearby` ⭐ 최우선

- [ ] **SecurityConfig에 `permitAll()` 경로 추가** (`/api/v1/places/**`, `/api/v1/search/**`, `/api/v1/directions/**`)
- [ ] Redis Geospatial 초기 데이터 적재 로직 (테스트용 `@PostConstruct` 또는 data.sql)
  - `GEOADD geo:places {lng} {lat} {placeId}` 형식
- [ ] `PlaceQueryRepository` — QueryDSL Haversine 반경 쿼리 구현 (캐시 Miss 시 fallback)
- [ ] `PlaceService.findNearby(lat, lng, radius, cursor, size)` 구현
  1. 캐시 키 생성: 좌표 소수점 3자리 반올림 → `nearby:{lat}:{lng}:{radius}`
  2. Redis `GEORADIUS` 조회 → Hit 시 반환
  3. Miss → DB Haversine 쿼리 → 결과 캐싱 (TTL 5분)
- [ ] `PlaceController.getNearbyPlaces()` 구현
- [ ] `PlaceNearbyResponse` DTO 작성 (placeId, name, category, imageUrl, latitude, longitude, rating, reviewCount, likeCount, distanceMeters)

### ~~1-2. 관광지 상세 조회 `GET /api/v1/places/{placeId}`~~ ✅ 완료

- [x] `PlaceService.findById(placeId)` 구현
  1. Redis 캐시 조회 (key: `place:{placeId}`, TTL 10분)
  2. Miss → DB 조회 → 캐싱
  3. 조회수: `INCR place:view:{placeId}` (Atomic)
- [x] `PlaceDetailResponse` DTO 작성 (isLiked는 비로그인 시 false 고정)
- [x] `PlaceController.getPlaceDetail()` 구현

### ~~1-3. 길찾기 `GET /api/v1/directions`~~ ✅ 완료

- [x] `DirectionService.buildKakaoMapUrl(originLat, originLng, destLat, destLng)` 구현
  - 서버가 경로 계산 안 함 → KakaoMap redirect URL만 생성
  - KakaoMap API Key는 `.env` + `application.yml`에 환경변수로 관리
  - API 장애 시 `PLACE_007` 에러
- [x] `DirectionController.getDirections()` 구현
- [x] `application.yml`에 `kakao.map.api-key: ${KAKAO_MAP_API_KEY}` 추가
- [x] `.env`, `.env.example`에 `KAKAO_MAP_API_KEY` 추가

---

## PHASE 2 — 검색 기능

> Redis ZSet이 핵심. 모두 인증 불필요.

### 2-1. 인기 검색어 `GET /api/v1/search/popular` ⭐

- [ ] `SearchService.getPopularKeywords()` 구현
  - `ZREVRANGE search:ranking 0 9 WITHSCORES`
  - 이전 순위 `search:ranking:prev`와 비교 → changeType(UP/DOWN/SAME/NEW) 계산
- [ ] `PopularKeywordResponse` DTO 작성 (rank, keyword, searchCount, changeType)
- [ ] `SearchController.getPopularKeywords()` 구현
- [ ] 자정 score 초기화 스케줄러 구현
  ```java
  @Scheduled(cron = "0 0 0 * * *")
  public void resetDailyRanking() { ... }
  ```

### 2-2. 관광지 검색 `GET /api/v1/search/places`

- [ ] `PlaceQueryRepository.searchByKeyword(keyword, category, region, cursor, size)` — QueryDSL `name LIKE %keyword%`
- [ ] `SearchService.searchPlaces(keyword, ...)` 구현
  - 검색 시 `ZINCRBY search:ranking 1 {keyword}` 집계
- [ ] `SearchPlaceResponse` DTO 작성
- [ ] `SearchController.searchPlaces()` 구현

### 2-3. 축제 검색 `GET /api/v1/search/festivals`

- [ ] `SearchService.searchFestivals(keyword, ...)` 구현 (박경화님이 만든 Festival 관련 엔티티 연동)
  - 검색 시 `ZINCRBY search:ranking 1 {keyword}` 집계
- [ ] `SearchFestivalResponse` DTO 작성
- [ ] `SearchController.searchFestivals()` 구현

### 2-4. 검색어 자동완성 `GET /api/v1/search/suggest`

- [ ] `SearchService.suggest(prefix)` 구현
  - DB: `name LIKE {prefix}%` LIMIT 5
  - Redis ZSet prefix 매칭 병행 (선택)
  - 최대 5개 반환
- [ ] `SearchController.suggest()` 구현

---

## PHASE 3 — 인증 연동 후 개발 (정민교 JWT 필터 완료 후)

> `@AuthenticationPrincipal`로 `userId` 추출 가능해지면 시작.

### 3-1. 관광지 찜하기 / 취소

- [ ] `POST /api/v1/places/{placeId}/like`
  - `UserLike` INSERT + `INCR place:like:{placeId}`
  - 중복 찜 → `PLACE_002` 에러
- [ ] `DELETE /api/v1/places/{placeId}/like`
  - `UserLike` DELETE + `DECR place:like:{placeId}`
  - 찜 안 한 경우 → `PLACE_003` 에러
- [ ] 상세조회 API에서 로그인 사용자 `isLiked` 포함 반환 처리

### 3-2. 최근 검색어 저장 / 조회

- [ ] `POST /api/v1/search` — `LPUSH search:recent:{userId} {keyword}` + `LTRIM` (최대 10개)
- [ ] `GET /api/v1/search/recent` — `LRANGE search:recent:{userId} 0 -1`

---

## PHASE 4 — P1 추천 및 보조 기능

### 4-1. 인기·위치 기반 추천

- [ ] `GET /api/v1/recommend/popular` — Redis ZSet `recommend:popular` TOP 10 (찜수+조회수 가중합산)
- [ ] `GET /api/v1/recommend/nearby` — Redis Geospatial 반경 내 랜덤 샘플링
- [ ] `GET /api/v1/recommend/category` — 카테고리 필터 + 평점 내림차순

### 4-2. 관광지 기반 추천 `GET /api/v1/places/{placeId}/recommend`

- [ ] 동일 카테고리 + 근거리 장소 TOP 5 반환 (캐시 TTL 30분)

### 4-3. 주변 상점 조회 (신현민 shops 테이블 완료 후)

- [ ] `GET /api/v1/places/{placeId}/nearby-shops` — shops 테이블 Geospatial 조회

---

## PHASE 5 — 관리자 API (정민교와 협의 후)

- [ ] `POST /api/v1/admin/places` — 관광지 등록 (ADMIN)
- [ ] `PATCH /api/v1/admin/places/{placeId}` — 관광지 수정 (ADMIN)
- [ ] `DELETE /api/v1/admin/places/{placeId}` — 관광지 숨김/삭제 (ADMIN)
- [ ] 관광지 등록 시 Redis Geospatial에도 자동 적재

---

## 📋 현재 코드베이스 현황 요약

```
✅ 이미 있음
  - ApiResponse, GlobalExceptionHandler, ErrorCode, BusinessException
  - JpaAuditingConfig (@EnableJpaAuditing)
  - SecurityConfig (기본 구조)
  - application.yml / application-local.yml (Redis 설정 포함)
  - docker-compose.yml (MySQL 8.4 + Redis 7)
  - Account(users) Entity, AccountRepository, SignupService, UserAuthController

❌ 아직 없음 (내가 만들어야 함)
  - QueryDSL 의존성 (build.gradle)
  - RedisConfig Bean
  - Place / Review / UserLike Entity
  - PlaceRepository / ReviewRepository / UserLikeRepository
  - PlaceService / SearchService / DirectionService
  - PlaceController / SearchController / DirectionController
  - 내 도메인 ErrorCode (PLACE_001~007, SEARCH_001~005)
  - 모든 DTO (Request / Response)
  - KakaoMap API 연동 로직
  - Redis Geospatial / ZSet 로직
```
